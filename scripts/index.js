// Import Modules
/*import { getSentencesList, toggleOriginalSentenceVisibility, showSentence } from "./sentence_management.js";

export const scrambledSentenceElement = document.getElementById("scambled-sentence");
export const sentenceElement = document.getElementById("sentence");
export const userGuessInputElement = document.getElementById("user-guess");
export const getButton = document.getElementById('getButton');

getButton.addEventListener('click', showSentence);

export let scrambledSentence = [];
export let sentencesList = [];
export let waitingDone = false;
export let sentenceVisible = true;
export let sentence = '';

function checkGuess(event) {
    if (event.key === 'Enter') {
        if (!sentenceVisible) {
            toggleOriginalSentenceVisibility();
        } else {
            showSentence();
        }
    }
}

userGuessInputElement.addEventListener('keydown', checkGuess);

getSentencesList();

showSentence();
*/

